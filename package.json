{
  "name": "dental-adventure-sites",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build": "node scripts/ensure-app-deps.mjs && npm run build --prefix app && node scripts/build-sites-static.mjs"
  }
}
