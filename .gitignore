.env
node_modules/

# local voice auditions (generated, never shipped)
voice-auditions/

# playwright MCP scratch output
.playwright-mcp/

# raw generator exports — tens of MB each, and reproducible from the source art
art-in/models-raw/

# raw drops that land at the repo root
/*.glb
/*.zip

# generated helper lines (audition output, never shipped as-is)
voice-lines/

# visual-gate screenshots (regenerate with scripts/shots.mjs)
shots/
